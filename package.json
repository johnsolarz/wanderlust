{
  "name": "wanderlust",
  "version": "1.0.0",
  "description": "Uses fetch, async, and await to request information from the Foursquare API and APIXU to create a travel website.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib"
  },
  "author": "John Solarz",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
